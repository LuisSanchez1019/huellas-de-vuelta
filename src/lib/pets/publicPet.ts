/**
 * Ruta pública de una mascota / placa QR. La URL que codifica un QR físico se construye SOLO con
 * `qrPublicUrl` (`lib/qr/qrBaseUrl.ts`): usa el dominio oficial configurado, nunca el del navegador.
 */
export const PUBLIC_PET_PATH = "/m";
